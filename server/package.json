{
	"name": "server",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"start": "node server.js",
		"dev": "nodemon server.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"git": "git add . && git commit -m 'changes' && git pull origin server && git push origin server"
	},
	"author": "",
	"license": "ISC",
	"dependencies": {
		"body-parser": "^1.19.2",
		"cors": "^2.8.5",
		"dotenv": "^16.0.0",
		"express": "^4.17.3",
		"method-override": "^3.0.0",
		"mysql": "^2.18.1",
		"nodemon": "^2.0.15",
		"routes": "^2.1.0"
	}
}
